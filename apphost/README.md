# Patchline AppHost

TypeScript Aspire AppHost for local Patchline orchestration.

This folder follows Aspire's existing JavaScript/TypeScript app pattern: the root repository keeps `aspire.config.json`, and the AppHost package lives in this subfolder.

Edit `apphost.mts` for local resources. Do not edit `.aspire/modules`; Aspire regenerates those files.
