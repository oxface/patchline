# n8n Workflows

Export n8n workflows here as JSON.

Planned first workflows:

- `intake-telegram-to-github.json`
- `intake-evaluation.json`

Real secrets must stay in n8n credentials or local secret stores. Workflow JSON should reference credentials by name or placeholder only.
