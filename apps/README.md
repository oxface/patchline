# Apps

Application code lives here when a workflow earns custom implementation.

Current plan:

- `server/`: future Python/FastAPI/Microsoft Agent Framework slice.
- `web/`: future TanStack frontend.

The first milestone should remain n8n-native, so these folders are placeholders until extraction is justified.
