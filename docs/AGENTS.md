# Docs Agent Instructions

Documentation in this folder is durable project context.

When editing docs:

- Keep facts in one canonical place.
- Prefer short references over duplicated explanations.
- Add architectural decisions to `docs/decisions/`.
- Keep roadmap changes in `docs/roadmap.md`.
- Keep observed n8n friction in `docs/n8n-friction-log.md`.
