# Behaviors

This folder contains concrete workflow behavior specifications.

Use behavior specs for workflows that are stable enough to describe and evaluate. Architectural choices belong in `docs/decisions/`.
