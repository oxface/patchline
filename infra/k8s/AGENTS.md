# k8s Agent Instructions

Do not add Kubernetes manifests until the local runtime and n8n-native workflow have been exercised.

When this folder becomes active, target a single-node k3s cluster on a Hyper-V Ubuntu VM first.
