# Argo CD Agent Instructions

Do not add Argo CD application definitions before k3s manifests exist.

Keep GitOps configuration focused on deploying the local learning environment, not production hardening.
