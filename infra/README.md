# Infrastructure

Deployment infrastructure lives here.

Current sequence:

1. k3s manifests after the local workflow shape is understood.
2. Argo CD definitions after k3s manifests exist.

The Aspire TypeScript AppHost lives in `../apphost/`.
